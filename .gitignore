# Build artifacts
/forwardproxy
*.test
*.out
coverage.*

# Runtime data — never commit real config, logs, or audit trails
data/
config.json
config.json.bak
activity.jsonl
audit.log

# Environment & editor / OS noise
.env
*.log
.DS_Store

# Prebuilt image export (publish via GitHub Releases, not the repo)
release/*.tar.gz
