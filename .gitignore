# Build artifacts
/forwardproxy
*.test
*.out
coverage.*

# Runtime data — never commit real config, logs, or audit trails
data/
config.json
config.json.bak
activity.jsonl
audit.log

# Environment & editor / OS noise
.env
*.log
.DS_Store

# Prebuilt images live in the repo (release/*.tar.gz) so they can be loaded
# directly with `docker load` on targets that cannot build.
